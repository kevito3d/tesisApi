/* import { Router } from 'express';
const router = Router();

import {
    createParish,
    getAll,
    getOne,
   setOne,
   deleteOne
} from '../../controllers/references/parishController';


router.get('/', getAll );

router.get('/:id', getOne);

router.post('/', createParish);

router.put('/:id', setOne);

router.delete('/:id', deleteOne);

export default router; */